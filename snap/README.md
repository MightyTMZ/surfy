# Surfy snap animation

A dependency-free visual prototype for Surfy’s “snap to start” moment.

Open `index.html` directly in a browser, or serve the repository and visit `/snap/`:

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000/snap/>. Click the gauntlet, click the button, or press Space to run the animation.
